# 通用功能剩余 P1/P2 模块开发任务分解表（基于 tyteam 组织）
## 说明
- 所有包统一前缀 `@tyteam/`，基于 ESM 规范（`package.json` 含 `"type": "module"`），适配已发布包的模块一致性。
- 依赖版本优先选择最新稳定版，确保与 `eslint@^9.39.1` 无冲突，CLI 功能均集成到已发布的 `@tyteam/ty-cli`（包名已确认）。
- 步骤拆解到“复制命令+修改配置”级别，可直接落地执行。

| 模块（优先级）                | 具体开发步骤                                                                                                                                                                                                 | 预期输出物                                                                                                                                                                                                 | 技术栈/工具细节                                                                                                                                                                                                 |
|-------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 1. @tyteam/stylelint-config（P1） | 1. 在 packages 目录新建 `stylelint-config`，执行 `pnpm init`；<br>2. 修改 package.json：<br>   ```json<br>   {<br>     "name": "@tyteam/stylelint-config",<br>     "version": "1.0.0",<br>     "type": "module",<br>     "main": "index.js",<br>     "peerDependencies": { "stylelint": "^16.9.0" },<br>     "dependencies": { "stylelint-config-standard": "^36.0.0", "stylelint-order": "^6.0.4" },<br>     "devDependencies": { "stylelint": "^16.9.0" },<br>     "files": ["index.js", "README.md"]<br>   }<br>   ```<br>3. 编写 index.js（Stylelint flat config）：<br>   ```javascript<br>   import orderPlugin from "stylelint-order";<br>   export default [<br>     {<br>       plugins: { "order": orderPlugin },<br>       extends: ["stylelint-config-standard"],<br>       rules: {<br>         "no-important": "error", // 禁止 !important<br>         "selector-class-pattern": ["^ty-[a-z0-9-]+$", { "message": "类名需以 ty- 开头，仅含小写字母、数字、中划线" }],<br>         "order/order": ["custom-properties", "declarations"],<br>         "order/properties-alphabetical-order": true // 样式属性按字母序排列<br>       }<br>     }<br>   ];<br>   ```<br>4. 测试：新建 test.css 写不规范样式（如 `.MyClass { color: red !important; }`），执行 `npx stylelint test.css` 验证报错；<br>5. 编写 README：说明安装命令、配置方式；<br>6. 发布：`npm login` → `pnpm publish --access public`。 | 1. 已发布的 `@tyteam/stylelint-config` npm 包；<br>2. 测试用例及验证报告；<br>3. 含安装/配置说明的 README。                                                                                                 | - stylelint 版本 `^16.9.0`（支持 ESM + flat config）；<br>- 核心依赖：`stylelint-config-standard`（基础规则）、`stylelint-order`（控制样式顺序）；<br>- 团队规则可按需调整（如类名前缀、属性顺序）。                                                                             |
| 2. @tyteam/prettier-config（P1）  | 1. 在 packages 目录新建 `prettier-config`，执行 `pnpm init`；<br>2. 修改 package.json：<br>   ```json<br>   {<br>     "name": "@tyteam/prettier-config",<br>     "version": "1.0.0",<br>     "type": "module",<br>     "main": "index.js",<br>     "peerDependencies": { "prettier": "^3.3.3" },<br>     "devDependencies": { "prettier": "^3.3.3" },<br>     "files": ["index.js", "README.md"]<br>   }<br>   ```<br>3. 编写 index.js：<br>   ```javascript<br>   export default {<br>     printWidth: 100, // 每行最大字符数<br>     tabWidth: 2, // 缩进 2 空格<br>     singleQuote: true, // 单引号<br>     trailingComma: "es5", // 对象/数组末尾加逗号<br>     semi: true, // 语句结尾加分号<br>     arrowParens: "avoid" // 单参数箭头函数省略括号（(a) => {} → a => {}）<br>   };<br>   ```<br>4. 测试：新建 test.js 写不规范代码，执行 `npx prettier --config index.js --write test.js` 验证格式化生效；<br>5. 编写 README：说明集成方式（如 `prettier.config.js` 导入）；<br>6. 发布：`pnpm publish --access public`。 | 1. 已发布的 `@tyteam/prettier-config` npm 包；<br>2. 格式化测试用例；<br>3. 含集成说明的 README。                                                                                                             | - prettier 版本 `^3.3.3`（支持 ESM 配置）；<br>- 规则与 `@tyteam/eslint-config-base` 无冲突（如均为单引号、2 空格缩进）；<br>- 支持项目自定义覆盖（如 `prettier.config.js` 中扩展）。                                                                                       |
| 3. @tyteam/ty-cli - team-cli lint fix 命令（P1） | 1. 进入 `@tyteam/ty-cli` 项目目录，安装依赖：`pnpm add stylelint eslint -D`（用于 CLI 内部调用）；<br>2. 在 `bin/commands/lint.js` 中新增 `fix` 子命令（基于 commander）：<br>   ```javascript<br>   import { program } from "commander";<br>   import { execSync } from "child_process";<br>   program<br>     .command("fix [dir]")<br>     .description("一键修复 ESLint 和 Stylelint 问题（默认修复整个项目）")<br>     .action((dir = ".") => {<br>       try {<br>         // 执行 ESLint 修复<br>         execSync(`npx eslint ${dir} --fix`, { stdio: "inherit" });<br>         // 执行 Stylelint 修复（若项目已集成）<br>         try {<br>           execSync(`npx stylelint ${dir}/**/*.{css,scss,vue} --fix`, { stdio: "inherit" });<br>         } catch (e) { /* 未集成 Stylelint 则忽略 */ }<br>         console.log("\n✅ 修复完成！");<br>       } catch (e) {<br>         console.log("\n❌ 部分问题需手动修复，请查看上述报错信息");<br>       }<br>     });<br>   export default program;<br>   ```<br>3. 在 `bin/index.js` 中导入并注册该命令：<br>   ```javascript<br>   import lintCommand from "./commands/lint.js";<br>   program.addCommand(lintCommand);<br>   ```<br>4. 测试：在已集成规范的项目中执行 `node bin/index.js lint fix src/`，验证 ESLint/Stylelint 可修复问题被自动修复；<br>5. 更新 `@tyteam/ty-cli` 版本（如 1.1.0），发布补丁：`pnpm publish --access public`。 | 1. 支持 `team-cli lint fix [dir]` 命令的 `@tyteam/ty-cli@1.1.0`；<br>2. 命令可单独修复 ESLint 问题，若项目集成 Stylelint 则同时修复；<br>3. 补充命令说明到 CLI 的 README。                                                                                                       | - 依赖 `commander@^12.0.0`（已在 CLI 中集成）；<br>- 用 `child_process.execSync` 调用本地项目的 ESLint/Stylelint（避免 CLI 强依赖版本）；<br>- 兼容“仅集成 ESLint”“同时集成两者”的项目场景。                                                                                   |
| 4. @tyteam/ty-cli - team-cli check-update 命令（P1） | 1. 进入 `@tyteam/ty-cli` 目录，安装依赖：`pnpm add npm-registry-client@^8.6.0`；<br>2. 在 `bin/commands/check-update.js` 中编写命令：<br>   ```javascript<br>   import { program } from "commander";<br>   import Client from "npm-registry-client";<br>   const client = new Client({});<br>   const registry = "https://registry.npmjs.org/";<br>   // 需要检测的核心包<br>   const corePackages = ["@tyteam/ty-cli", "@tyteam/eslint-config-base", "@tyteam/stylelint-config"];<br>   program<br>     .command("check-update")<br>     .description("检测 CLI 及关联规范包的最新版本")<br>     .action(async () => {<br>       console.log("🔍 正在检测最新版本...");<br>       for (const pkg of corePackages) {<br>         await new Promise((resolve) => {<br>           client.get(`${registry}${pkg}`, (err, data) => {<br>             if (err) { console.log(`❌ 检测 ${pkg} 失败：${err.message}`); resolve(); return; }<br>             const latestVersion = data["dist-tags"].latest;<br>             // 获取本地安装的版本（若已安装）<br>             const localVersion = require(`../../node_modules/${pkg}/package.json`).version || "未安装";<br>             console.log(`\n${pkg}：`);<br>             console.log(`  本地版本：${localVersion}`);<br>             console.log(`  最新版本：${latestVersion}`);<br>             if (localVersion !== "未安装" && localVersion < latestVersion) {<br>               console.log(`  📌 可更新：pnpm add ${pkg}@latest -D`);<br>             }<br>             resolve();<br>           });<br>         });<br>       }<br>     });<br>   export default program;<br>   ```<br>3. 在 `bin/index.js` 中注册命令；<br>4. 测试：执行 `node bin/index.js check-update`，验证能正确显示本地与最新版本，且提示更新命令；<br>5. （可选）在 CLI 启动时自动触发检测（添加到 `bin/index.js` 入口）；<br>6. 发布 `@tyteam/ty-cli@1.2.0`。 | 1. 支持 `team-cli check-update` 命令的 `@tyteam/ty-cli@1.2.0`；<br>2. 能检测核心规范包的版本差异，提供明确更新命令；<br>3. 补充命令说明到 README。                                                                                                                         | - 依赖 `npm-registry-client` 查询 npm 仓库版本（避免手动请求 API）；<br>- 核心包列表可后续扩展（如新增 `@tyteam/prettier-config`）；<br>- 版本对比使用字符串比较（适合语义化版本）。                                                                                             |
| 5. @tyteam/ty-cli - 存量项目修复（team-cli lint init 命令）（P2） | 1. 进入 `@tyteam/ty-cli` 目录，在 `bin/commands/lint-init.js` 中编写命令：<br>   ```javascript<br>   import { program } from "commander";<br>   import fs from "fs/promises";<br>   import path from "path";<br>   import { execSync } from "child_process";<br>   program<br>     .command("lint init")<br>     .description("存量项目一键接入团队规范")<br>     .action(async () => {<br>       const cwd = process.cwd();<br>       const pkgPath = path.join(cwd, "package.json");<br>       const pkg = require(pkgPath);<br>       // 1. 询问项目类型（Vue3/其他）<br>       const { projectType } = await inquirer.prompt([{<br>         type: "list",<br>         name: "projectType",<br>         message: "请选择项目类型",<br>         choices: ["Vue3", "其他"]<br>       }]);<br>       // 2. 安装规范包<br>       const eslintConfig = projectType === "Vue3" ? "@tyteam/eslint-config-vue3" : "@tyteam/eslint-config-base";<br>       console.log(`📦 正在安装规范包：${eslintConfig}、@tyteam/stylelint-config`);<br>       execSync(`pnpm add ${eslintConfig} @tyteam/stylelint-config eslint stylelint -D`, { stdio: "inherit" });<br>       // 3. 生成 eslint.config.js<br>       await fs.writeFile(path.join(cwd, "eslint.config.js"), `import tyteamConfig from "${eslintConfig}"; export default [...tyteamConfig];`);<br>       // 4. 生成 stylelint.config.js<br>       await fs.writeFile(path.join(cwd, "stylelint.config.js"), `import tyteamConfig from "@tyteam/stylelint-config"; export default [...tyteamConfig];`);<br>       // 5. 添加脚本到 package.json<br>       pkg.scripts = {<br>         ...pkg.scripts,<br>         "lint": "eslint . && stylelint ./**/*.{css,scss,vue}",<br>         "lint:fix": "team-cli lint fix"<br>       };<br>       await fs.writeFile(pkgPath, JSON.stringify(pkg, null, 2));<br>       console.log("✅ 存量项目规范接入完成！");<br>     });<br>   export default program;<br>   ```<br>2. 在 `bin/index.js` 中注册命令；<br>3. 测试：在未接入规范的旧项目中执行 `node bin/index.js lint init`，验证能自动安装依赖、生成配置文件、添加脚本；<br>4. 发布 `@tyteam/ty-cli@1.3.0`。 | 1. 支持 `team-cli lint init` 命令的 `@tyteam/ty-cli@1.3.0`；<br>2. 存量项目可一键安装依赖、生成配置、添加脚本，无需手动操作；<br>3. 补充命令说明到 README（含存量项目使用场景）。                                                                                           | - 依赖 `inquirer@^9.2.22`（已在 CLI 中集成，用于交互询问）；<br>- 自动识别项目类型，匹配对应的 ESLint 配置包；<br>- 兼容 pnpm/npm/yarn（可通过询问用户包管理器优化，当前默认 pnpm）。                                                                                   |
| 6. @tyteam/ty-cli - husky 通用集成（P1） | 1. 进入 `@tyteam/ty-cli` 目录，安装依赖：`pnpm add husky@^9.0.11 -D`；<br>2. 在 `bin/commands/init.js` 中扩展交互逻辑（添加是否集成提交规范选项）：<br>   ```javascript<br>   // 在原有 init 命令的 inquirer 提问中新增：<br>   {<br>     type: "confirm",<br>     name: "useCommitlint",<br>     message: "是否集成提交规范（husky + @tyteam/commitlint-config）？",<br>     default: true<br>   }<br>   ```<br>3. 在 init 命令的 action 中添加集成逻辑：<br>   ```javascript<br>   if (answers.useCommitlint) {<br>     // 安装依赖<br>     execSync(`pnpm add @tyteam/commitlint-config @commitlint/cli husky -D`, { stdio: "inherit" });<br>     // 生成 commitlint.config.js<br>     await fs.writeFile("commitlint.config.js", `import tyteamConfig from "@tyteam/commitlint-config"; export default [...tyteamConfig];`);<br>     // 初始化 husky 并添加 commit-msg 钩子<br>     execSync(`npx husky install`, { stdio: "inherit" });<br>     execSync(`npx husky add .husky/commit-msg 'npx --no -- commitlint --edit $1'`, { stdio: "inherit" });<br>     // 添加 prepare 脚本（自动启用 husky）<br>     pkg.scripts = { ...pkg.scripts, "prepare": "husky install" };<br>     await fs.writeFile("package.json", JSON.stringify(pkg, null, 2));<br>   }<br>   ```<br>4. 测试：执行 `team-cli init` 选择集成提交规范，验证能安装依赖、生成配置、创建 husky 钩子；<br>5. 发布 `@tyteam/ty-cli@1.4.0`。 | 1. 支持集成 husky+commitlint 的 `@tyteam/ty-cli@1.4.0`；<br>2. 初始化项目时可一键开启提交规范校验，无需手动配置 husky；<br>3. 补充提交规范集成说明到 README。                                                                                                             | - husky 版本 `^9.0.11`（支持 ESM，无需额外配置）；<br>- 自动生成 `commitlint.config.js` 并关联 `@tyteam/commitlint-config`；<br>- 添加 `prepare` 脚本，确保团队成员安装依赖后自动启用 husky 钩子。                                                                             |

## 关键执行注意事项
1. 版本管理：每个 CLI 功能开发完成后，执行 `npm version [patch/minor]`（如新增命令用 `minor` 升级版本），再发布，确保语义化版本规范。
2. 依赖兼容：所有新增依赖需确认支持 ESM（如 `stylelint@^16.x`、`husky@^9.x`），避免与已发布包的模块系统冲突。
3. 发布权限：确保发布时已登录 npm 账号，且属于 `tyteam` 组织（执行 `npm whoami` 验证），私有包需添加 `--access restricted`（当前默认公开包用 `--access public`）。
4. 测试覆盖：每个命令需在“新建项目”“存量项目”“仅集成部分规范的项目”三种场景下测试，确保无报错。

### 下一步交付物提议
完成以上模块后，通用功能工具链已完整覆盖“代码规范+样式规范+格式化+提交规范+CLI 工具+存量修复”，可进入微前端子应用专属功能开发。如需继续，我可帮你生成 **《微前端子应用专属功能开发任务分解表》**，聚焦 `@tyteam/template-vue3-qiankun` 模板包和 CLI 模板扩展