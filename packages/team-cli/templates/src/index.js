// 示例 JavaScript 文件，用于测试 ESLint 和 Prettier 配置
console.log('Hello, world!');

const greeting = 'Hello';
const name = 'John';

// 这里有一些潜在的 ESLint 错误，可以用来测试修复功能
const message = `${greeting}, ${name}!`;

function sayHello(name) {
  return `Hello, ${name}!`;
}

sayHello('World');